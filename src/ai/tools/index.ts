import {
  holdButtonTool,
  muteTool,
  screenshotTool,
  unmuteTool,
} from './bizhawkTools';

export const toolSet = {
  mute: muteTool,
  unmute: unmuteTool,
  screenshot: screenshotTool,
  holdButton: holdButtonTool,
};
